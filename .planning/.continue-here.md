---
context: default
phase: none
status: in_progress
last_updated: 2026-06-18T09:20:54Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: gcloud global config — `gcloud config set account` affects ALL terminals. Always use named configurations. Personal = `tapcard-personal`, Work = `default`.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| gcloud account bleed | Setting active account in one terminal breaks all others | blocking | Use `gcloud config configurations activate tapcard-personal` instead of `gcloud config set account` |

<current_state>
TapCard product repo is live at github.com/mohdrezwan/tapcard-product. CI is green at v1.0.3. Installer enhanced to 10 steps. All code complete. Session paused before running first end-to-end install test on tapcard-demo GCP project.
</current_state>

<completed_work>

- GitHub repo created at mohdrezwan/tapcard-product (personal account)
- Docker Hub secrets set (DOCKERHUB_USERNAME, DOCKERHUB_TOKEN)
- CI fixed and green — v1.0.2 and v1.0.3 released (Docker image + frontend dist + GitHub release)
- README.md written
- backend/.env.example added
- 18 new tests added (auth/google, auth/exchange, profile GET/POST, vcard GET, health) — 34 total passing
- initStaffMap exported from index.js so vcard tests can seed staffMap
- setup.sh enhanced 8→10 steps: step 3 auto-enables GCP APIs, step 4 auto-inits Firebase, step 5 auto-opens browser for OAuth
- All 18 commits rewritten to mohdrezwan <mohamadrezwan@gmail.com>
- gcloud config `tapcard-personal` created (mohamadrezwan@gmail.com, project: tapcard-demo)
- tapcard-demo GCP project created on personal account with billing enabled
</completed_work>

<remaining_work>

- **HIGH: Run end-to-end installer test** on tapcard-demo (personal GCP project)
- Medium: Add sample staff CSV to installer/seed/ for customer demo
- Medium: docs/ customer guide (staff CSV format, theme customisation, admin roles)
- Low: CHANGELOG.md
- Low: Custom domain DNS auto-setup in setup.sh
- Low: Docker Hub full description (requires manual update — PAT only has Read & Write, no admin scope)
</remaining_work>

<decisions_made>

- Personal GitHub account (mohdrezwan) used for this repo — not work account (mp-git-rezwan)
- Docker Hub: mohdrezwan/tapcard-backend
- setup.sh kept as bash script (not web UI) — user installs for customers himself
- gcloud named configurations used to isolate personal vs work GCP contexts
- tapcard-demo project on personal GCP (mohamadrezwan@gmail.com) for isolated testing
- git history rewritten — all commits now show mohdrezwan as author
</decisions_made>

<blockers>

- Installer test paused: other work terminal was running subsync-pipeline on work account — activating personal config broke it
</blockers>

## Required Reading (in order)
1. `CLAUDE.md` — project overview, backend architecture, env vars
2. `installer/setup.sh` — 10-step wizard (recently updated)
3. `README.md` — customer-facing docs

## Infrastructure State

- **github.com/mohdrezwan/tapcard-product** — public, master branch, v1.0.3 latest release
- **mohdrezwan/tapcard-backend** on Docker Hub — v1.0.2 image (v1.0.3 CI was building at pause)
- **tapcard-demo** GCP project — created, billing linked, empty (no install run yet)
- **gcloud config `tapcard-personal`** — mohamadrezwan@gmail.com, project tapcard-demo
- **gcloud config `default`** — rezwan@mediaprima.com.my, project mp-git-rezwan (work)
- **GitHub secrets** — DOCKERHUB_USERNAME + DOCKERHUB_TOKEN set on repo

<context>
Productizing TapCard for sale on personal account. Codebase complete. Just need to validate the installer works end-to-end on a real GCP project before first customer. All the hard work is done — this is final QA before selling.
</context>

<next_action>
1. Ensure work terminal is stable: `gcloud config configurations activate default`
2. In a dedicated terminal for TapCard testing: `gcloud config configurations activate tapcard-personal`
3. Run: `cd /Users/rezwan/claude/tapcard-product && bash installer/setup.sh`
4. Project ID when prompted: `tapcard-demo`, region: `asia-southeast1`
5. After successful install, delete project: `gcloud projects delete tapcard-demo --quiet`
</next_action>
